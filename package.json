{
  "name": "boids",
  "version": "0.0.0",
  "private": true,
  "description": "Testing a maths utility library with a quick round of boids",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "browserify -d -t babelify src/index.js > dist/index.js",
    "watch": "watch 'npm run build' src"
  },
  "author": "Matt Styles",
  "license": "ISC",
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^11.0.1",
    "watch": "^0.16.0"
  },
  "dependencies": {
    "core-js": "^1.0.1",
    "mathutil": "^0.1.0",
    "quay": "^0.2.0",
    "raf": "^3.1.0"
  }
}
